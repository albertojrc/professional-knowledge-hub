import { capstoneProjects } from './capstoneProjects'
export const capstoneSearchEntries = capstoneProjects.map((project) => ({ id:`search-${project.id}`, title:project.title, kind:'Capstone Project' as const, area:'Capstone Projects', category:project.area, summary:project.professionalGoal, tags:[project.level,project.duration,project.businessQuestion,...project.requiredInputs,...project.workflow,...project.formulas,...project.models,...project.outputs,...project.deliverables,...project.evaluationCriteria,...project.connectedModules,...project.practiceLinks,project.portfolioUse], targetView:'capstone-projects' as const }))
