import { academicUpgradeItems } from './academicUpgradePass'
export const academicUpgradeSearchEntries = academicUpgradeItems.map((item) => ({ id:`search-${item.id}`, title:item.title, kind:'Academic Upgrade' as const, area:'Academic Upgrade Pass', category:item.area, summary:item.upgradeAction, tags:[item.status,String(item.confidence),item.sourceFile,item.driveId,item.currentLimitation,item.sourceRule,item.nextStep,...item.targetStudyModules,...item.targetLessons,...item.targetAssets,...item.evidenceToExtract,...item.reviewQuestions], targetView:'academic-upgrade-pass' as const }))
